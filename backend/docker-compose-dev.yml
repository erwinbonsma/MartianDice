version: "3.7"

services:
  memcached:
    image: memcached:1.6

  game-service:
    image: martiandice-game-service
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/usr/backend/src
    tty: true
    ports:
      - 8765:8765
    depends_on:
      - memcached